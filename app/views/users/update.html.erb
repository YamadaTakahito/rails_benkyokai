<%= form_for @user do |f| %>

  <div class="field">
    <%= f.label :name, "名前" %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :mail, "メールアドレス" %>
    <%= f.text_field :mail %>
  </div>

  <div class="actions">
    <%= f.submit "登録する" %>
  </div>

<% end %>
